# ============================================================
# Grafana Dashboard 配置
# ============================================================
apiVersion: 1

providers:
  - name: "Exam Paper Generator"
    folder: "Application"
    type: file
    options:
      path: /var/lib/grafana/dashboards
